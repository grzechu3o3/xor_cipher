import randomimport stringdef generate_text_file(file_name, size_in_mb):    # Rozmiar w bajtach (1 MB = 1024 * 1024 bajtów)    size_in_bytes = size_in_mb * 1024 * 1024    chunk_size = 1024 * 1024  # 1 MB    with open(file_name, "w", encoding="utf-8") as f:        while size_in_bytes > 0:            # Tworzenie losowego tekstu            chunk_text = ''.join(random.choices(string.ascii_letters + string.digits + " ", k=min(chunk_size, size_in_bytes)))            f.write(chunk_text)            size_in_bytes -= len(chunk_text)if __name__ == "__main__":    file_name = "plik_tekstowy_50mb.txt"    size_in_mb = 500  # Rozmiar w MB    print(f"Generuję plik tekstowy o rozmiarze {size_in_mb} MB: {file_name}")    generate_text_file(file_name, size_in_mb)    print("Plik został wygenerowany.")